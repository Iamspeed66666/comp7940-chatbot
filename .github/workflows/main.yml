name: Deploy to AWS App Runner

on:
  push:
    branches:
      - main # 当代码推送到 main 分支时触发

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint with flake8
        run: |
          pip install flake8
          flake8 main.py --max-line-length=150 --exit-zero

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ secrets.AWS_ECR_REPOSITORY }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Deploy to AWS App Runner
        uses: aws-actions/aws-app-runner-deploy@v1
        id: deploy-app
        with:
          service: ${{ secrets.AWS_APP_RUNNER_SERVICE_ARN }}
          image: ${{ steps.build-image.outputs.image }}
          wait: true

      - name: Update App Runner Auto Scaling Configuration
        env:
          SERVICE_ARN: ${{ steps.deploy-app.outputs.service-arn }}
        run: |
          aws apprunner update-service \
            --service-arn $SERVICE_ARN \
            --auto-scaling-configuration-arn "arn:aws:apprunner:ap-east-1:你的账户ID:autoscalingconfiguration/你的配置名/版本号